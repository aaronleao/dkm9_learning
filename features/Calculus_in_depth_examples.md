# Calculus in depth examples

## Examples
REQUIREMENT: Create a very detailed step-by-step solution for this examples
REQUIREMENT: Use as much as plot, diagrams, geometric as possible

1. Limit for f(x) = x + a, a in R.
2. Limit for f(x) = ax^2 + bx +c, a,b,c \in R
3. Limit when x goes to 0 for f(x)=a\(x) + b/(x^2)
4. Limit for f(x) = sin(ax+b)
5. Add one transcendental examples

## Euler constant
Create a new example where the user can use his intuition and "derive" e (Euler's constant). I mean deireve without derivative.
Make this example very ludic, give plots, tell story, make modern example in financial or ecological growth.

## Derivative rules
1. Explain the notation d/dx, what that means. Why in Physics we have df/dx = y -> df = ydx, why we can "multiply de dx in both sides? What that mean mathmaticaly speaking?

2. Explain the following rules in exetremely step-by-step, don't ommit any step, make plot, diagrams add external links etc.
    1. d/dx c = 0
    2. d/dx cf(x) = cf'(x)
    3. d/dx (f(x) + g(x)) = df/fx + dg/dx
    4. d/dx (f(x) * g(x)) = f'(x)g(x) + g'(x)f(x)
    5. d/dx ( f(x)/g(x)) = (f'(x)g(x) - g'(x)f(x))/g(x)^2
    6. d/dx(f(g(x))) = f(g(x))*g'(x)

